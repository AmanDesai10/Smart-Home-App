import 'package:flutter/material.dart';
import 'package:meta/meta.dart';
import 'package:smart_home_app/models/add_room_model.dart';

class RoomData {
  static final List<AddRoom> roomlist = [
    // AddRoom(roomName: "Living Room", rDeviceList: rDeviceList)
  ];
}

//Left from here as need to make device list non-static
